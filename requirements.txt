SpeechRecognition>=3.8.1
pyttsx3>=2.90
# webbrowser is part of the Python stdlib
# For Windows: install pyaudio via pipwin to avoid build issues
# Example (PowerShell):
# pip install pipwin; pipwin install pyaudio
